# Local command (requires 1Password CLI):
# source venv/bin/activate && TOKEN=$(op item get o5xa5jyxlkboobcdwzw6n4ttoi --fields token --reveal) && python3 stars.py --token "$TOKEN" --user thecatfix --max 10

name: "Generate Markdown for Starred Github Repository."

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install requests
        run: pip install requests

      - name: Generate starred repos markdown
        run: |
          python stars.py --token ${{ secrets.GH_TOKEN }} --user thecatfix --output README.md --max 100

      - name: Create Pull Request
        run: |
        # Configure git for commits
        git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

        # Create a new branch for the PR
        BRANCH_NAME="update-starred-repos-$(date +%Y%m%d-%H%M%S)"

          # Commit changes to new branch
          git checkout -b "$BRANCH_NAME"
          git add README.md
          git commit -m "🤖 Update README.md with latest starred repos"

        # Push the branch
        git push origin "$BRANCH_NAME"

          # Create PR using GitHub CLI
          gh pr create \
            --title "🤖 Update starred repositories list" \
            --body "## 🤖 Automated Update

          This PR updates the README.md with the latest starred repositories from GitHub.

          ### Changes
          - Updated starred repositories list
          - Generated automatically every 6 hours

          ---
          *This PR was automatically created by the [stars.yml](.github/workflows/stars.yml) workflow.*" \
            --base main \
            --head "$BRANCH_NAME"
          
